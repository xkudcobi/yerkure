// Code generated by protoc-gen-ts-server. DO NOT EDIT.
// source: worldmonitor/safety/v1/service.proto

export interface GetTorontoSafetyRequest {
  dataset: string;
  limit: number;
  division: string;
  neighbourhood: string;
  offence: string;
  year: number;
}

export interface GetTorontoSafetyResponse {
  semantic: string;
  source: string;
  sourceLabel: string;
  attribution: string;
  disclaimer: string;
  sourceUrl: string;
  fetchedAt: number;
  sourceEditedAt: number;
  newestContentAt: number;
  newestContentYear: number;
  degraded: boolean;
  unavailable: boolean;
  matched: number;
  truncated: boolean;
  occurrences: TorontoReportedOccurrence[];
  aggregates: TorontoAnnualAggregate[];
}

export interface TorontoReportedOccurrence {
  id: string;
  objectId: number;
  eventUniqueId: string;
  reportDate: string;
  occurrenceDate: string;
  division: string;
  locationType: string;
  premisesType: string;
  offence: string;
  csiCategory: string;
  neighbourhood: string;
  longitude: number;
  latitude: number;
  approximate: boolean;
}

export interface TorontoAnnualAggregate {
  id: string;
  objectId: number;
  eventYear: number;
  divisionOriginal: string;
  divisionFinal: string;
  neighbourhood: string;
  eventCount: number;
  incidentPoint: boolean;
}

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface ServerContext {
  request: Request;
  pathParams: Record<string, string>;
  headers: Record<string, string>;
}

export interface ServerOptions {
  onError?: (error: unknown, req: Request) => Response | Promise<Response>;
  validateRequest?: (methodName: string, body: unknown) => FieldViolation[] | undefined;
}

export interface RouteDescriptor {
  method: string;
  path: string;
  handler: (req: Request) => Promise<Response>;
}

export interface SafetyServiceHandler {
  getTorontoSafety(ctx: ServerContext, req: GetTorontoSafetyRequest): Promise<GetTorontoSafetyResponse>;
}

export function createSafetyServiceRoutes(
  handler: SafetyServiceHandler,
  options?: ServerOptions,
): RouteDescriptor[] {
  return [
    {
      method: "GET",
      path: "/api/safety/v1/get-toronto-safety",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: GetTorontoSafetyRequest = {
            dataset: params.get("dataset") ?? "",
            limit: Number(params.get("limit") ?? "0"),
            division: params.get("division") ?? "",
            neighbourhood: params.get("neighbourhood") ?? "",
            offence: params.get("offence") ?? "",
            year: Number(params.get("year") ?? "0"),
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("getTorontoSafety", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.getTorontoSafety(ctx, body);
          return new Response(JSON.stringify(result as GetTorontoSafetyResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
  ];
}

