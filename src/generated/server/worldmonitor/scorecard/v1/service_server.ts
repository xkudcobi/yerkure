// Code generated by protoc-gen-ts-server. DO NOT EDIT.
// source: worldmonitor/scorecard/v1/service.proto

export interface GetFiveFactorScorecardRequest {
  countryCode: string;
}

export interface GetFiveFactorScorecardResponse {
  scorecard?: FiveFactorCountryScorecard;
  unavailable: boolean;
  unavailableReason: string;
}

export interface FiveFactorCountryScorecard {
  countryCode: string;
  methodologyVersion: string;
  computedAt: string;
  pillars: FiveFactorPillar[];
}

export interface FiveFactorPillar {
  pillar: string;
  hasScore: boolean;
  score: number;
  subScore: number;
  band: string;
  inputCoverage: number;
  aggregationMethod: string;
  inputs: ScorecardEvidence[];
  insufficientReasons: string[];
  includedMembers: string[];
  excludedMembers: ExcludedBlocMember[];
  memberWeights: ScorecardMemberWeight[];
}

export interface ScorecardEvidence {
  inputId: string;
  available: boolean;
  value: number;
  hasValue: boolean;
  year: number;
  unit: string;
  source: string;
  sourceKey: string;
  unavailableReason: string;
  quality: string;
  observations: ScorecardObservation[];
  countryCode: string;
}

export interface ScorecardObservation {
  name: string;
  value: number;
  year: number;
  unit: string;
  source: string;
  indicatorCode: string;
}

export interface ExcludedBlocMember {
  countryCode: string;
  reason: string;
}

export interface ScorecardMemberWeight {
  countryCode: string;
  populationMillions: number;
  hasPopulation: boolean;
}

export interface ListFiveFactorScorecardsRequest {
}

export interface ListFiveFactorScorecardsResponse {
  scorecards: FiveFactorCountryScorecardSummary[];
  unavailable: boolean;
  unavailableReason: string;
  methodologyVersion: string;
  computedAt: string;
}

export interface FiveFactorCountryScorecardSummary {
  countryCode: string;
  pillars: FiveFactorPillarSummary[];
}

export interface FiveFactorPillarSummary {
  pillar: string;
  hasScore: boolean;
  score: number;
  subScore: number;
  band: string;
  inputCoverage: number;
  insufficientReasons: string[];
}

export interface GetBlocScorecardRequest {
  preset: string;
  members: string[];
}

export interface GetBlocScorecardResponse {
  scorecard?: FiveFactorBlocScorecard;
  unavailable: boolean;
  unavailableReason: string;
}

export interface FiveFactorBlocScorecard {
  id: string;
  label: string;
  methodologyVersion: string;
  computedAt: string;
  members: string[];
  includedMembers: string[];
  excludedMembers: ExcludedBlocMember[];
  pillars: FiveFactorPillar[];
}

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface ServerContext {
  request: Request;
  pathParams: Record<string, string>;
  headers: Record<string, string>;
}

export interface ServerOptions {
  onError?: (error: unknown, req: Request) => Response | Promise<Response>;
  validateRequest?: (methodName: string, body: unknown) => FieldViolation[] | undefined;
}

export interface RouteDescriptor {
  method: string;
  path: string;
  handler: (req: Request) => Promise<Response>;
}

export interface ScorecardServiceHandler {
  getFiveFactorScorecard(ctx: ServerContext, req: GetFiveFactorScorecardRequest): Promise<GetFiveFactorScorecardResponse>;
  listFiveFactorScorecards(ctx: ServerContext, req: ListFiveFactorScorecardsRequest): Promise<ListFiveFactorScorecardsResponse>;
  getBlocScorecard(ctx: ServerContext, req: GetBlocScorecardRequest): Promise<GetBlocScorecardResponse>;
}

export function createScorecardServiceRoutes(
  handler: ScorecardServiceHandler,
  options?: ServerOptions,
): RouteDescriptor[] {
  return [
    {
      method: "GET",
      path: "/api/scorecard/v1/get-five-factor-scorecard",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: GetFiveFactorScorecardRequest = {
            countryCode: params.get("countryCode") ?? "",
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("getFiveFactorScorecard", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.getFiveFactorScorecard(ctx, body);
          return new Response(JSON.stringify(result as GetFiveFactorScorecardResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/scorecard/v1/list-five-factor-scorecards",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const body = {} as ListFiveFactorScorecardsRequest;

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.listFiveFactorScorecards(ctx, body);
          return new Response(JSON.stringify(result as ListFiveFactorScorecardsResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/scorecard/v1/get-bloc-scorecard",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: GetBlocScorecardRequest = {
            preset: params.get("preset") ?? "",
            members: params.getAll("members"),
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("getBlocScorecard", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.getBlocScorecard(ctx, body);
          return new Response(JSON.stringify(result as GetBlocScorecardResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
  ];
}

