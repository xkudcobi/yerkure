// Code generated by protoc-gen-ts-client. DO NOT EDIT.
// source: worldmonitor/scorecard/v1/service.proto

export interface GetFiveFactorScorecardRequest {
  countryCode: string;
}

export interface GetFiveFactorScorecardResponse {
  scorecard?: FiveFactorCountryScorecard;
  unavailable: boolean;
  unavailableReason: string;
}

export interface FiveFactorCountryScorecard {
  countryCode: string;
  methodologyVersion: string;
  computedAt: string;
  pillars: FiveFactorPillar[];
}

export interface FiveFactorPillar {
  pillar: string;
  hasScore: boolean;
  score: number;
  subScore: number;
  band: string;
  inputCoverage: number;
  aggregationMethod: string;
  inputs: ScorecardEvidence[];
  insufficientReasons: string[];
  includedMembers: string[];
  excludedMembers: ExcludedBlocMember[];
  memberWeights: ScorecardMemberWeight[];
}

export interface ScorecardEvidence {
  inputId: string;
  available: boolean;
  value: number;
  hasValue: boolean;
  year: number;
  unit: string;
  source: string;
  sourceKey: string;
  unavailableReason: string;
  quality: string;
  observations: ScorecardObservation[];
  countryCode: string;
}

export interface ScorecardObservation {
  name: string;
  value: number;
  year: number;
  unit: string;
  source: string;
  indicatorCode: string;
}

export interface ExcludedBlocMember {
  countryCode: string;
  reason: string;
}

export interface ScorecardMemberWeight {
  countryCode: string;
  populationMillions: number;
  hasPopulation: boolean;
}

export interface ListFiveFactorScorecardsRequest {
}

export interface ListFiveFactorScorecardsResponse {
  scorecards: FiveFactorCountryScorecardSummary[];
  unavailable: boolean;
  unavailableReason: string;
  methodologyVersion: string;
  computedAt: string;
}

export interface FiveFactorCountryScorecardSummary {
  countryCode: string;
  pillars: FiveFactorPillarSummary[];
}

export interface FiveFactorPillarSummary {
  pillar: string;
  hasScore: boolean;
  score: number;
  subScore: number;
  band: string;
  inputCoverage: number;
  insufficientReasons: string[];
}

export interface GetBlocScorecardRequest {
  preset: string;
  members: string[];
}

export interface GetBlocScorecardResponse {
  scorecard?: FiveFactorBlocScorecard;
  unavailable: boolean;
  unavailableReason: string;
}

export interface FiveFactorBlocScorecard {
  id: string;
  label: string;
  methodologyVersion: string;
  computedAt: string;
  members: string[];
  includedMembers: string[];
  excludedMembers: ExcludedBlocMember[];
  pillars: FiveFactorPillar[];
}

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface ScorecardServiceClientOptions {
  fetch?: typeof fetch;
  defaultHeaders?: Record<string, string>;
}

export interface ScorecardServiceCallOptions {
  headers?: Record<string, string>;
  signal?: AbortSignal;
}

export class ScorecardServiceClient {
  private baseURL: string;
  private fetchFn: typeof fetch;
  private defaultHeaders: Record<string, string>;

  constructor(baseURL: string, options?: ScorecardServiceClientOptions) {
    this.baseURL = baseURL.replace(/\/+$/, "");
    this.fetchFn = options?.fetch ?? globalThis.fetch;
    this.defaultHeaders = { ...options?.defaultHeaders };
  }

  async getFiveFactorScorecard(req: GetFiveFactorScorecardRequest, options?: ScorecardServiceCallOptions): Promise<GetFiveFactorScorecardResponse> {
    let path = "/api/scorecard/v1/get-five-factor-scorecard";
    const params = new URLSearchParams();
    if (req.countryCode != null && req.countryCode !== "") params.set("countryCode", String(req.countryCode));
    const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "GET",
      headers,
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as GetFiveFactorScorecardResponse;
  }

  async listFiveFactorScorecards(_req: ListFiveFactorScorecardsRequest, options?: ScorecardServiceCallOptions): Promise<ListFiveFactorScorecardsResponse> {
    let path = "/api/scorecard/v1/list-five-factor-scorecards";
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "GET",
      headers,
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as ListFiveFactorScorecardsResponse;
  }

  async getBlocScorecard(req: GetBlocScorecardRequest, options?: ScorecardServiceCallOptions): Promise<GetBlocScorecardResponse> {
    let path = "/api/scorecard/v1/get-bloc-scorecard";
    const params = new URLSearchParams();
    if (req.preset != null && req.preset !== "") params.set("preset", String(req.preset));
    if (req.members && req.members.length > 0) req.members.forEach(v => params.append("members", v));
    const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "GET",
      headers,
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as GetBlocScorecardResponse;
  }

  private async handleError(resp: Response): Promise<never> {
    const body = await resp.text();
    if (resp.status === 400) {
      try {
        const parsed = JSON.parse(body);
        if (parsed.violations) {
          throw new ValidationError(parsed.violations);
        }
      } catch (e) {
        if (e instanceof ValidationError) throw e;
      }
    }
    throw new ApiError(resp.status, `Request failed with status ${resp.status}`, body);
  }
}

