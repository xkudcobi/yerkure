---
title: "中国数据覆盖"
description: "查看 WorldMonitor 中国数据覆盖清单，了解官方宏观、政策执法、台海活动、企业披露、互联互通、物流走廊与活动即时预测的数据来源、更新频率和访问边界。区分已上线、明确阻止、过时和不可用的来源契约，通过清单核查各健康端点如何报告传输与内容状态，避免将任务近期运行误读为中国数据完整、新鲜或健康。"
---

本页是 WorldMonitor 中国数据的总入口。它说明已上线和明确阻止的来源契约，
并不表示每个上游来源此刻都健康。当前传输与内容健康通过 `/api/health`
中的中国覆盖投影呈现。`/api/seed-health` 只报告中国覆盖 evaluator
本身是否近期运行；新鲜 evaluator heartbeat 不证明中国内容健康。

## 覆盖清单

| 管线 | 来源与方法 | 主要界面 | 更新契约 |
| --- | --- | --- | --- |
| 全球中国投影 | BIS、IMF、中国能源脊柱、UN Comtrade reporter 156、CCFI、中国市场指数、中国新闻源、八个审核航空枢纽、HKO 与西太平洋灾害 | 中国国家摘要与 operator 健康 | 按来源更新；每小时评估 |
| 官方宏观 | NBS 与 SAFE 的修订感知观测；PBoC 和 GACC 候选项保持显式不可用 | REST 快照与 Pro MCP `get_economic_data` 的 `china-macro` dataset | 36 小时 |
| 官方发布日历 | NBS 发布日历，以及由 ChinaMoney/CFETS 公告核验的暂定 PBoC LPR 日期 | 中国宏观 bootstrap 与 Pro MCP `get_economic_data` 的 `china-release-calendar` dataset | 36 小时 |
| 政策与执法 | CAC、SAMR、MIIT、MOFCOM、NDRC、PBOC 官方文件 | 中国国家简报与 `chinaPolicyEvents` bootstrap | 6 小时 |
| 台海活动 | 台湾国防部每日声明；日本统合幕僚监部背景单独归因 | Force Posture 与中国决策信号 | 3 小时 |
| 企业披露 | 经审核发行人篮子的 SSE、SZSE 公共元数据；HKEX 明确被阻止 | `chinaCorporateDisclosures` bootstrap 与中国决策信号 | 30 分钟 |
| 互联互通成交额与两融 | SSE、SZSE 关于北向成交额与融资融券余额的公共汇总统计 | `market:china:stock-connect:v1` 快照与中国健康投影 | 60 分钟 |
| 物流走廊 | 由六个具名来源族组成的四条审核走廊 | REST/UI 详情；MCP 接收受限决策信号摘要 | 按请求；健康聚合缓存 5 分钟 |
| 活动 nowcast | 官方宏观版本与七个审核代理指标族的确定性比较 | REST/UI 详情；MCP 接收受限决策信号摘要 | 按请求；健康结果缓存 15 分钟 |
| 决策信号组合 | 宏观、政策、台海、披露、走廊与 nowcast 的受限组合 | 国家简报、公共 RPC、bootstrap 与 Pro MCP | 15 分钟 |
| 双边 HS4 连续性 | 审核国家/战略产品注册表的年度 UN Comtrade 进口篮 | PRO `GET /api/supply-chain/v1/get-country-products` 与航线暴露工作流 | `0 6 1 * *` |

## 健康投影注册表

中国健康投影跟踪以下稳定契约。`launched` 表示该管线已纳入中国投影，
但不会覆盖它的实时新鲜度或内容状态。

| 契约 ID | 覆盖 | 上线状态 |
| --- | --- | --- |
| `economic.bis-policy` | BIS 政策利率 | `launched` |
| `economic.imf-macro` | IMF 宏观快照 | `launched` |
| `energy.jodi-oil` | JODI 石油 | `blocked` — `CHINA_UPSTREAM_ROW_UNAVAILABLE` |
| `energy.jodi-gas` | JODI 天然气 | `blocked` — `CHINA_UPSTREAM_ROW_UNAVAILABLE` |
| `energy.spine` | 中国能源脊柱 | `launched` |
| `trade.comtrade-reporter-156` | UN Comtrade reporter 156 | `launched` |
| `supply-chain.ccfi` | 中国出口集装箱运价指数 | `launched` |
| `market.china-index` | 中国国家市场指数 | `launched` |
| `market.china-corporate-disclosures` | SSE/SZSE 官方企业披露 | `launched` |
| `market.china-stock-connect` | SSE/SZSE 互联互通北向成交额与融资融券余额 | `launched` |
| `news.china` | 中国新闻摘要 | `launched` |
| `aviation.china-hubs` | 八个审核中国航空枢纽 | `launched` |
| `macro.china-snapshot` | 规范化中国宏观快照 | `launched` |
| `macro.china-release-calendar` | 中国发布日历 | `launched` |
| `policy.official-events` | 官方政策与执法事件 | `launched` |
| `hazards.western-pacific-cyclones` | 西太平洋气旋身份 | `launched` |
| `hazards.hko-warnings` | 香港天文台警报 | `launched` |
| `military.cross-strait-activity` | 官方台海活动基线 | `launched` |

JODI 仍可用于现有全球能源产品。其中国石油与天然气契约保持阻止，
因为审核后的上游国家索引目前没有发布实质性的 `CN` 行。因此，
健康的全球 JODI heartbeat 不能被呈现为中国覆盖。

详细方法与来源契约：

- [官方宏观与政策](/zh/china-official-macro-policy)
- [企业披露](/zh/china-corporate-disclosures)
- [物流走廊](/zh/china-logistics-corridors)
- [跨领域决策信号](/zh/china-decision-signals)
- [活动 nowcast 方法](/zh/methodology/china-activity-nowcast)
- [共享溯源契约](/zh/decision-signal-provenance)

## 状态与缺失值

来源请求成功不等于内容可用。中国健康检查会分别评估传输新鲜度和内容新鲜度。
成功请求仍可能发布过期、部分、空或时间戳未知的内容。

公共契约使用显式状态：

- `available` 或 `healthy` 表示所需负载通过领域验证和新鲜度规则；
- `partial` 或 `degraded` 表示部分审核覆盖缺失、过期或无效，但其他证据仍可用；
- `stale` 表示最后有效观测超出内容预算；
- `unavailable` 表示没有可发布的有效观测；
- `blocked` 表示来源被刻意禁止上线，通常因为 robots、条款、传输或来源实质性审核未通过。

缺失类别绝不会被转换为零、正常、不变或当前。保留最后良好值时，会保留原始
观测时间，并同时报告当前传输失败。

## 互联互通成交额与两融

`market:china:stock-connect:v1` 由四个官方交易所端点组成。它们位于企业披露
条款审核已覆盖的两个交易所主机上，四个端点均按 `admitted_aggregate_statistics`
准入：仅使用公共汇总市场统计，不含任何投资者层面或委托层面数据。

| 来源 ID | 交易所 | 序列 | 端点 |
| --- | --- | --- | --- |
| `sse-northbound` | SSE | 北向成交额 | `https://query.sse.com.cn/commonSoaQuery.do`，sqlId 为 `FW_HGTZL_HGTSCSJ_HGTCJGK_MRTJ`（沪股通成交概况） |
| `sse-margin` | SSE | 融资融券余额 | `https://query.sse.com.cn/marketdata/tradedata/queryMargin.do`（融资融券汇总） |
| `szse-northbound` | SZSE | 北向成交额 | `https://www.szse.cn/api/report/ShowReport/data`，CATALOGID 为 `SGT_SGTJYRB`（深股通交易日报） |
| `szse-margin` | SZSE | 融资融券余额 | `https://www.szse.cn/api/report/ShowReport/data`，CATALOGID 为 `1837_xxpl`（融资融券交易总量） |

审核条款记录与企业披露管线相同：SSE
[官方法律条款](https://www.sse.com.cn/home/legal/)，robots.txt 未发布（404）；
SZSE [适用规则与公告](https://www.szse.cn/application/laws/)，robots.txt
存在但为空（200）。

北向净流入不可用，因为该来源并不发布它。两个交易所自 2024-08-16 起停止披露
北向买卖拆分，只有总成交额保留下来。负载对此直接说明：`northbound.netFlow` 为
`{ status: 'unavailable', reason: 'EXCHANGE_STOPPED_PUBLISHING_BUY_SELL_SPLIT' }`，
并附带 `netFlowDiscontinuedOn: '2024-08-16'`。成交额是买入与卖出合计的双向
交易活动，不是流入或流出内地市场的资金，绝不可被读作资金流。

其余契约：

- 所有发布值统一规范化为 CNY。SSE 的融资融券本身以元计价；其余数字以
  亿（1e8）或万（1e4）到达并完成换算；
- 只有当两个交易所报告相同交易日时，才发布 SSE 与 SZSE 的合计值；否则合计值
  以 `TRADE_DATE_MISMATCH` 原因降级。该检查同时充当冻结探测器：某个交易所
  停止推进时立即可见，而不必等待新鲜度预算耗尽；
- 融资融券按 T+1 延迟发布，北向在每个交易时段收盘后发布；
- SZSE 端点按日期取数，因此 seeder 依据交易所交易日历
  （`https://www.szse.cn/api/report/exchange/onepersistenthour/monthList`）
  固定日期，并向前回溯有限数量的交易日进行探测；
- 该管线作为 `seed-bundle-market-backup` Railway bundle 的成员每小时运行。

## 双边 HS4 进口篮

双边存储覆盖一份共享 HS4 目录：`scripts/shared/comtrade-strategic-products.json`
中经过审核的 `bilateralHs4Code`，加上 `scripts/shared/supply-vulnerability-commodities.json`
中每种商品的 HS4 品目。定时 seeder 与按需公共回退请求同一目录，分两次请求，每次最多
20 个品目；回退还可以只请求单个品目，见下文。每个国家分片包含按 HS4 产品划分的年度进口额和主要出口伙伴，
用于国家暴露、航线影响、多部门冲击以及 PRO `get-country-products` 路由。
详细双边数据行不进入匿名中国摘要。

认证 Comtrade 路由对两个 HS4 批次分别发送一个从 `Y-2` 到 `Y-5` 的四年窗口。
规范化器按“产品 + 伙伴”保留最新年份，因此响应不会静默混合不同年份。
缺少 `COMTRADE_API_KEYS` 时，公共 preview 路由不接受年份列表，因此依次尝试
`Y-2` 和 `Y-3`。

生产计划被明确限制在配额内：

| 控制项 | 契约 |
| --- | --- |
| Railway cron | `0 6 1 * *` — 每月第一天 06:00 UTC |
| 提供商配额 | 每月 500 次带 key 请求 |
| Seeder 请求预算 | 480 |
| 新鲜度门禁 | 24 天；仅显式 `FORCE_RESEED=true` 可绕过 |
| 国家负载 TTL | 40 天 |
| 健康过期阈值 | 35 天 |
| 覆盖下限 | 110 个国家分片 |

低于 110 个分片的运行写入 `partial` 元数据，而不会声称完全成功。失败 reporter
最多连续两次运行保留已有分片；之后允许分片过期，使按需公共回退可以再次探测。
限流、配额耗尽和请求预算耗尽会发布部分覆盖并停止运行，不会用空负载覆盖最后良好数据。

### 仅聚合行

无论定时还是按需，每个双边请求都把第二伙伴、运输方式和海关程序固定为聚合值
（`partner2Code=0`、`motCode=0`、`customsCode=C00`）。没有这些参数时，Comtrade
会按“伙伴 × 第二伙伴 × 运输方式 × 海关程序”返回数据行，约为每个伙伴九行。
真正填满公共 preview 路由 500 行上限的是这种行倍增，而不是品目数量：德国的单个品目
就能独自触顶，因此整目录回退恰好对用户最关心的大进口国以 `incomplete` 结束。
加上这些参数后，同一请求返回每个伙伴一行，外加报告的 World 合计行。
分组本就只保留每个伙伴的聚合行，因此存储值不变；下降的是行数，而 World 合计行从此始终存在。

### 存储的键

规范国家分片 `comtrade:bilateral-hs4:{ISO2}:v1` 保持一贯形状：每个品目的前五个
来源，含金额与份额，以及分母口径。所有派生评分器——进口集中度、航线暴露、
咽喉要道指数——都读取该列表并对其全部求和，因此列表变长会在没有任何方法变更可解释的
情况下改变已发布评分。此外，两个批量 seeder 通过按键数量（而非字节数）分批的 pipeline
读取全部国家分片，因此规范负载新增的任何内容都会落入一个无人约束的响应体。
所以更深的证据无法搭载在该键上，而是存放在一个同级键中。

`comtrade:bilateral-hs4-partners:{ISO2}:v1` 按品目保存未取整份额达到分母 1% 的
全部来源；为使只有单一主导供应方的品目仍有上下文，至少补足到五个来源，
并且无论份额高低最多 25 个。被该规则排除的来源以数量和合计份额一并发布，
使读取方能说明自己未展示多少贸易量，而不是暗示列表已完整。每个来源带有以千克计的
净重，以及提供方报告时的数量及其单位，单位取自固定注册表
`scripts/shared/comtrade-quantity-units.json`。Comtrade 用 0 表示未报告重量，
因此 0 被存储为缺失，而不是确认为零。该同级键与国家分片共享 40 天 TTL，
以及相同的保留与 TTL 延长规则。只有 `get-country-products` 读取它。

`comtrade:world-exports-hs4:v1` 是每次运行一份的全局快照：记录每个 reporter 对每个
受审品目的出口。按品目保存观测年份、在该年份申报并按出口额排序的 reporter 列表
（每行含美元金额和已报告的净重），以及 `unrankedReporterCount`：该品目最新申报
早于这一年份的 reporter 数量。这些延迟申报方或已停止出口的 reporter 不参与排名，
因此排名只是该年份申报方之间的排名，并可能在它们申报后变化：简报将其写作
"rank N of M reporters filing YEAR"，并注明未排名的数量。运行会在尝试第一个 reporter 之前，就为它在请求预算中
预留两次请求——每个目录批次一次，省略 reporter 并把伙伴固定为 World，因此单次请求
即可覆盖全部 reporter。先预留可以让逐 reporter 的预算预检保持正确，并避免预算中止
把该快照整个丢掉。它共享 40 天 TTL。其结果写入 seed metadata 并通过
`/api/seed-health` 公布；`observed` 以外的任何状态都是覆盖缺口，因为供应方的绝对
规模及其世界排名只来自这一个键。只有 `get-country-products` 读取它，且仅在请求了
具体品目时。只有当快照中该品目的观测年份与所服务行的年份相同时，才会给来源附加
规模：延迟申报方的旧行不会带上规模，而不是带上一个并非其年份的排名。

### 单品目恢复

当 PRO 读取方请求已存分片不包含的品目时，按需公共回退只请求该品目，而不是整个目录。
正因如此，恢复才对大进口国有效——它们的整目录抓取会触顶 preview 上限。
结果记录在逐品目 sentinel `comtrade:bilateral-hs4-lazy-heading:{ISO2}:{HS4}:v1` 中，
因此一个品目的失败不会压制同一国家另一个品目的恢复。单品目恢复从不写入规范分片：
一个品目不是一个国家进口篮；恢复得到的品目带有自己的抓取时间，而不是分片的抓取时间。
观测年份早于已存年份的恢复结果会被拒绝。

在这些证据出现之前写入的分片没有同级键，也没有世界出口数据。它们继续提供前五个来源，
读取这些分片的界面把缺失的体量与规模标注为不可用。获取更深证据不需要强制重新 seed，
下一次定时运行即会写入。

## 访问边界

- 国家摘要、走廊状态、决策信号摘要、溯源与聚合健康通过受限公共界面提供。
- 详细走廊控制塔与活动 nowcast 证据只通过 REST/UI 提供。MCP 没有这两个路由的
  专用工具；Pro MCP 通过 `get_china_decision_signals` 接收其受限摘要。
- Pro MCP 通过 `get_economic_data` 的 `china-macro` 或
  `china-release-calendar` dataset 读取规范宏观与发布日历缓存。
- 详细双边 HS4 产品行和航线暴露仅限 PRO。
- Pro MCP 决策信号工具获得与公共组合相同的受限条目和溯源，不获得原始交易所文件或不受限来源扩展。
- 代理凭据、逐次请求原始诊断、缓存名称和详细新鲜度阈值仅限 operator。
  公共文档只说明配置键与选择语义，不公开其值。

官方发布者身份不等于独立验证。发布者身份、提取置信度、分类置信度、佐证和
新鲜度始终是独立声明。
