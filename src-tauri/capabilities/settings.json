{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Capabilities for the first-party settings window",
  "windows": ["settings"],
  "permissions": ["core:default"]
}
